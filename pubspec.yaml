name: sleep_seek_mobile
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:

    sdk: flutter
  http: ^0.12.2
  provider: ^4.3.2+4
  logging: ^0.11.4
  flutter_clean_architecture: ^3.0.1
  dartz: ^0.9.2
  modal_progress_hud: ^0.1.2
  flutter_secure_storage: ^3.3.5
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    # images
    - assets/img/
    - assets/img/stay/properties/
    - assets/img/accommodation/properties/